/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://www.bedazzledwcs.co.uk", // replace with your real domain
  generateRobotsTxt: true, // also generates robots.txt
  sitemapSize: 5000,
};
